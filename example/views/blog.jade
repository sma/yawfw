extends base
block content
  .page-header
    h1 My Blog
  each object in objects
    h2= object.title
      =" "
      small= date(object.created, "D. MMMM YYYY, HH:mm")
    div!= markdown(object.content)